{
  "name": "tempo-site",
  "version": "0.1.0",
  "description": "Client site template — extends tempo-core Nuxt layer",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run --root ./tempo-core",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@nuxtjs/strapi": "^2.1.1",
    "marked": "^17.0.5",
    "stripe": "^20.4.1",
    "tempo-core": "file:./tempo-core",
    "zod": "^4.3.6",
    "zod-to-json-schema": "^3.25.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/js-yaml": "^4.0.9",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.1",
    "tsx": "^4.21.0"
  }
}
